{
  "name": "thyrotrack-server",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Standalone backend for ThyroTrack push notifications, PDF export, and lab-photo OCR. Deploy separately from the static frontend (Vercel/etc.) — this needs a persistent Node process for node-cron and SQLite.",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "pdfkit": "^0.15.0",
    "tesseract.js": "^5.1.1",
    "web-push": "^3.6.7"
  }
}
